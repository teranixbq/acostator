# Acostator — Arsitektur Sistem

## Gambaran Arsitektur

```
┌─────────────────────────────────────────────┐
│                   Browser                    │
│  ┌─────────────────────────────────────────┐│
│  │         React + Vite (SPA)              ││
│  │  halaman, komponen, UI anotasi          ││
│  └─────────────────────────────────────────┘│
└──────────────────────┬──────────────────────┘
                       │ HTTP / JSON API
┌──────────────────────▼──────────────────────┐
│         Cloudflare Workers (Hono)            │
│  ┌──────────────────────────────────────┐   │
│  │           Hono Routes                │   │
│  │  /api/projects  /api/annotations     │   │
│  │  /api/export    /api/auth            │   │
│  └──────────────────┬───────────────────┘   │
│  ┌──────────────────▼───────────────────┐   │
│  │            Service Layer             │   │
│  │  ProjectService  AnnotationService   │   │
│  │  CategoryService  ExportService      │   │
│  └──────────────────┬───────────────────┘   │
│  ┌──────────────────▼───────────────────┐   │
│  │         Drizzle ORM                  │   │
│  └──────────┬────────────┬─────────────┘   │
└─────────────┼────────────┼─────────────────┘
              │            │
   ┌──────────▼──┐   ┌─────▼──────┐
   │ D1 (SQLite) │   │     R2     │
   │  data utama │   │ file CSV   │
   └─────────────┘   └────────────┘
```

---

## Tech Stack

| Layer | Pilihan | Alasan |
|---|---|---|
| Runtime | Cloudflare Workers | Edge, serverless, binding native ke D1/R2 |
| API Framework | Hono | Ringan, dibuat untuk Workers, tanpa ketergantungan Node.js |
| Frontend | React + Vite (SPA) | Ekosistem familiar, tidak butuh SSR untuk annotation tool |
| Bahasa | TypeScript (strict) | Type safety untuk data model yang kompleks |
| Database | Cloudflare D1 (SQLite) | Relational, cocok untuk struktur project→row→quadruple |
| File Storage | Cloudflare R2 | Upload CSV hingga 1GB, tanpa egress fee |
| ORM | Drizzle ORM | Satu-satunya ORM dengan dukungan D1 first-class |
| Auth | GitHub OAuth (manual) | NextAuth tidak bisa jalan di Workers |
| Styling | Tailwind CSS v4 | Utility-first, iterasi UI cepat |
| UI Components | shadcn/ui | Accessible, customizable |
| Testing | Vitest + `@cloudflare/vitest-pool-workers` | Environment Workers native di test |
| Code Quality | Biome | Lint + format dalam satu tool, cepat |
| CI/CD | GitHub Actions | lint → typecheck → test → deploy preview |
| Deploy | Cloudflare Pages + Workers | Frontend di Pages, API di Workers |

---

## Struktur Monorepo

```
acostator/                          ← root (npm workspaces)
├── apps/
│   ├── web/                        ← React + Vite (SPA)
│   │   ├── src/
│   │   │   ├── pages/
│   │   │   │   ├── login.tsx
│   │   │   │   ├── dashboard.tsx        ← daftar project
│   │   │   │   └── projects/
│   │   │   │       ├── [id].tsx         ← overview project + daftar row
│   │   │   │       └── [id]/annotate/
│   │   │   │           └── [rowId].tsx  ← interface anotasi
│   │   │   ├── components/
│   │   │   │   ├── annotation/
│   │   │   │   │   ├── TextHighlighter.tsx
│   │   │   │   │   ├── QuadruplePanel.tsx
│   │   │   │   │   ├── AnnotationWizard.tsx
│   │   │   │   │   ├── CategorySelector.tsx
│   │   │   │   │   └── ImplicitToggle.tsx
│   │   │   │   ├── project/
│   │   │   │   │   ├── ProjectCard.tsx
│   │   │   │   │   ├── ProgressBar.tsx
│   │   │   │   │   ├── RowList.tsx
│   │   │   │   │   └── UploadForm.tsx
│   │   │   │   └── ui/              ← komponen shadcn/ui
│   │   │   ├── lib/
│   │   │   │   ├── api.ts           ← typed API client
│   │   │   │   └── auth.ts          ← state auth
│   │   │   └── main.tsx
│   │   ├── vite.config.ts
│   │   └── package.json
│   └── worker/                     ← Hono + Cloudflare Workers
│       ├── src/
│       │   ├── index.ts             ← entry point Hono app
│       │   ├── routes/
│       │   │   ├── auth.ts
│       │   │   ├── projects.ts
│       │   │   ├── annotations.ts
│       │   │   └── export.ts
│       │   ├── services/
│       │   │   ├── project.service.ts
│       │   │   ├── annotation.service.ts
│       │   │   ├── category.service.ts
│       │   │   └── export.service.ts
│       │   ├── db/
│       │   │   ├── schema.ts        ← Drizzle schema
│       │   │   └── migrations/
│       │   └── middleware/
│       │       └── auth.ts          ← validasi session
│       ├── test/
│       ├── wrangler.jsonc
│       └── package.json
├── packages/
│   └── shared/                     ← TypeScript types bersama
│       ├── src/
│       │   └── types.ts             ← Quadruple, Project, dll
│       └── package.json
├── .github/
│   └── workflows/
│       └── ci.yml                  ← lint → typecheck → test → deploy
├── biome.json
└── package.json                    ← root npm workspaces
```

---

## Modul Utama (Deep Design)

### TextHighlighter
Interface terkecil, implementasi terbesar. Inti dari tool ini.

```typescript
interface TextHighlighterProps {
  text: string
  highlights: Highlight[]
  activeHighlight: ActiveHighlight | null
  mode: 'aspect' | 'opinion' | 'idle'
  onSelect: (selection: TextSelection) => void
  onClear: (highlightId: string) => void
}
```

### AnnotationWizard (State Machine)

```
idle → selecting_aspect → aspect_selected → selecting_opinion
     → opinion_selected → selecting_sentiment → selecting_category → idle
```

Transisi yang tidak valid di-ignore, bukan di-throw sebagai error.

### CategoryService
```typescript
interface CategoryService {
  getRecommendations(db: D1Database, projectId: string, query: string): Promise<Category[]>
  createCategory(db: D1Database, projectId: string, name: string): Promise<Category>
  incrementUsage(db: D1Database, categoryId: string): Promise<void>
}
```

---

## Alur Upload CSV

```
Client upload file (maks 1GB) langsung ke R2 via presigned URL
  → Worker validasi: format CSV, keberadaan kolom
  → Worker baca dari R2, streaming parse dengan Papa Parse
  → Insert DatasetRows dalam batch (500 row per transaksi D1)
  → Update Project.total_rows
  → Return project ID → redirect ke halaman project
```

Upload langsung ke R2 menghindari batas request body Workers 100MB.

---

## Strategi Simpan Anotasi

Setiap quadruple langsung disimpan saat step category selesai — tanpa "submit" di akhir. Mencegah kehilangan data jika browser ditutup di tengah anotasi.

Status row berubah ke `completed` hanya ketika user secara eksplisit menekan "Tandai Selesai".

---

## Alur Export

```
User klik Export (JSON/CSV)
  → Worker query semua Quadruples untuk project dari D1
  → Join dengan DatasetRows
  → Transform ke format output
  → Stream response sebagai file download
```

---

## Alur Auth (GitHub OAuth — manual)

```
User klik "Login with GitHub"
  → Redirect ke GitHub OAuth
  → GitHub redirect ke /api/auth/callback?code=...
  → Worker tukar code dengan access token
  → Worker fetch profil user GitHub
  → Worker buat/update User di D1
  → Worker set signed session cookie (JWT)
  → Redirect ke /dashboard
```

---

## CI/CD Pipeline (GitHub Actions)

```
push / PR
  └─ biome lint + format check
  └─ tsc --noEmit (typecheck)
  └─ vitest (unit test dengan Workers pool)
  └─ deploy ke Cloudflare Pages preview (saat PR)
  └─ deploy ke production (saat merge ke main)
```
