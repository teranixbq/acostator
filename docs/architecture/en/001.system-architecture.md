# Acostator — System Architecture

## Architecture Overview

```
┌─────────────────────────────────────────────┐
│                   Browser                    │
│  ┌─────────────────────────────────────────┐│
│  │         React + Vite (SPA)              ││
│  │  pages, components, annotation UI       ││
│  └─────────────────────────────────────────┘│
└──────────────────────┬──────────────────────┘
                       │ HTTP / JSON API
┌──────────────────────▼──────────────────────┐
│         Cloudflare Workers (Hono)            │
│  ┌──────────────────────────────────────┐   │
│  │           Hono Routes                │   │
│  │  /api/projects  /api/annotations     │   │
│  │  /api/export    /api/auth            │   │
│  └──────────────────┬───────────────────┘   │
│  ┌──────────────────▼───────────────────┐   │
│  │            Service Layer             │   │
│  │  ProjectService  AnnotationService   │   │
│  │  CategoryService  ExportService      │   │
│  └──────────────────┬───────────────────┘   │
│  ┌──────────────────▼───────────────────┐   │
│  │         Drizzle ORM                  │   │
│  └──────────┬────────────┬─────────────┘   │
└─────────────┼────────────┼─────────────────┘
              │            │
   ┌──────────▼──┐   ┌─────▼──────┐
   │ D1 (SQLite) │   │     R2     │
   │  projects,  │   │ CSV files  │
   │  categories,│   │(permanent) │
   │ annotations │   │            │
   └─────────────┘   └────────────┘
```

---

## Tech Stack

| Layer | Choice | Rationale |
|---|---|---|
| Runtime | Cloudflare Workers | Edge, serverless, native D1/R2 bindings |
| API Framework | Hono | Lightweight, built for Workers, zero Node.js dependency |
| Frontend | React + Vite (SPA) | Familiar ecosystem, no SSR needed for annotation tool |
| Language | TypeScript (strict) | Type safety for the complex data model |
| Database | Cloudflare D1 (SQLite) | Relational, fits project→row→quadruple structure |
| File Storage | Cloudflare R2 | CSV upload up to 1GB, no egress fees |
| ORM | Drizzle ORM | Only ORM with first-class D1 support |
| Auth | GitHub OAuth (manual) | NextAuth doesn't run on Workers |
| Styling | Tailwind CSS v4 | Utility-first, fast UI iteration |
| UI Components | shadcn/ui | Accessible, customizable |
| Testing | Vitest + `@cloudflare/vitest-pool-workers` | Native Workers environment in tests |
| Code Quality | Biome | Lint + format in one tool, fast |
| CI/CD | GitHub Actions | lint → typecheck → test → deploy preview |
| Deploy | Cloudflare Pages + Workers | Frontend on Pages, API on Workers |

---

## Monorepo Structure

```
acostator/                          ← root (npm workspaces)
├── apps/
│   ├── web/                        ← React + Vite (SPA)
│   │   ├── src/
│   │   │   ├── pages/
│   │   │   │   ├── login.tsx
│   │   │   │   ├── dashboard.tsx        ← project list
│   │   │   │   └── projects/
│   │   │   │       ├── [id].tsx         ← project overview + row list
│   │   │   │       └── [id]/annotate/
│   │   │   │           └── [rowId].tsx  ← annotation interface
│   │   │   ├── components/
│   │   │   │   ├── annotation/
│   │   │   │   │   ├── TextHighlighter.tsx
│   │   │   │   │   ├── QuadruplePanel.tsx
│   │   │   │   │   ├── AnnotationWizard.tsx
│   │   │   │   │   ├── CategorySelector.tsx
│   │   │   │   │   └── ImplicitToggle.tsx
│   │   │   │   ├── project/
│   │   │   │   │   ├── ProjectCard.tsx
│   │   │   │   │   ├── ProgressBar.tsx
│   │   │   │   │   ├── RowList.tsx
│   │   │   │   │   └── UploadForm.tsx
│   │   │   │   └── ui/              ← shadcn/ui components
│   │   │   ├── lib/
│   │   │   │   ├── api.ts           ← typed API client
│   │   │   │   └── auth.ts          ← auth state
│   │   │   └── main.tsx
│   │   ├── vite.config.ts
│   │   └── package.json
│   └── worker/                     ← Hono + Cloudflare Workers
│       ├── src/
│       │   ├── index.ts             ← Hono app entry
│       │   ├── routes/
│       │   │   ├── auth.ts
│       │   │   ├── projects.ts
│       │   │   ├── annotations.ts
│       │   │   └── export.ts
│       │   ├── services/
│       │   │   ├── project.service.ts
│       │   │   ├── annotation.service.ts
│       │   │   ├── category.service.ts
│       │   │   └── export.service.ts
│       │   ├── db/
│       │   │   ├── schema.ts        ← Drizzle schema
│       │   │   └── migrations/
│       │   └── middleware/
│       │       └── auth.ts          ← session validation
│       ├── test/
│       ├── wrangler.jsonc
│       └── package.json
├── packages/
│   └── shared/                     ← shared TypeScript types
│       ├── src/
│       │   └── types.ts             ← Quadruple, Project, etc.
│       └── package.json
├── .github/
│   └── workflows/
│       └── ci.yml                  ← lint → typecheck → test → deploy
├── biome.json
└── package.json                    ← npm workspaces root
```

---

## Key Modules (Deep Design)

### TextHighlighter
Smallest interface, largest implementation. Core of the tool.

```typescript
interface TextHighlighterProps {
  text: string
  highlights: Highlight[]
  activeHighlight: ActiveHighlight | null
  mode: 'aspect' | 'opinion' | 'idle'
  onSelect: (selection: TextSelection) => void
  onClear: (highlightId: string) => void
}
```

Responsibilities:
- Render text with highlight overlay
- Handle mouse selection for spans
- Display existing highlights (aspect = color A, opinion = color B)
- Prevent invalid overlapping highlights

### AnnotationWizard (State Machine)

```
idle → selecting_aspect → aspect_selected → selecting_opinion
     → opinion_selected → selecting_sentiment → selecting_category → idle
```

Invalid transitions are ignored, not thrown as errors.

### CategoryService
```typescript
interface CategoryService {
  getRecommendations(db: D1Database, projectId: string, query: string): Promise<Category[]>
  createCategory(db: D1Database, projectId: string, name: string): Promise<Category>
  incrementUsage(db: D1Database, categoryId: string): Promise<void>
}
```

Recommendations sorted by `usage_count DESC`, then fuzzy match against query.

---

## CSV Upload Flow

```
Client uploads file (max 1GB) directly to R2 via presigned URL
  → Worker validates: CSV format, at least one column present
  → Worker saves file metadata to D1 (Project.file_key, Project.total_rows)
  → Worker returns column list to browser
  → User selects which column contains text to annotate (Column Picker)
  → Worker saves selected column as Project.text_column
  → Redirect to project page
```

No rows are inserted into D1 at upload time. The CSV stays in R2 permanently.
Direct-to-R2 upload avoids the Workers 100MB request body limit.

---

## Annotation Load Flow

```
User opens annotation page for a project
  → Browser fetches CSV from R2 via a signed URL (issued by Worker)
  → Browser parses CSV client-side with Papa Parse
  → Rows are read from the column matching Project.text_column
  → Annotation UI renders row text directly from parsed CSV
```

D1 is not queried for row text — all row content comes from R2 at runtime.

---

## Annotation Save Strategy

Each quadruple (Annotation) is saved to D1 immediately when the sentiment step completes — no "submit" at the end. Prevents data loss if the browser is closed mid-annotation.

Progress is derived from the count of distinct `row_index` values in the Annotation table — there is no separate row status field in D1.

---

## Export Flow

```
User clicks Export (JSON/CSV)
  → Worker fetches the original CSV from R2
  → Worker queries all Annotations for the project from D1
  → Worker joins CSV rows with Annotations by row_index
  → Transform to output format (JSON or CSV)
  → Stream response as file download
```

---

## Auth Flow (GitHub OAuth — manual)

```
User clicks "Login with GitHub"
  → Redirect to GitHub OAuth (github.com/login/oauth/authorize)
  → GitHub redirects to /api/auth/callback?code=...
  → Worker exchanges code for access token
  → Worker fetches GitHub user profile
  → Worker creates/updates User in D1
  → Worker sets signed session cookie (JWT)
  → Redirect to /dashboard
```

All API routes validate the session cookie via auth middleware.

---

## CI/CD Pipeline (GitHub Actions)

```
push / PR
  └─ biome lint + format check
  └─ tsc --noEmit (typecheck)
  └─ vitest (unit tests with Workers pool)
  └─ deploy to Cloudflare Pages preview (on PR)
  └─ deploy to production (on merge to main)
```
